import { MomentDurationHumanizePipe } from './moment-duration-humanize.pipe';

describe('MomentDurationHumanizePipe', () => {
  it('create an instance', () => {
    const pipe = new MomentDurationHumanizePipe();
    expect(pipe).toBeTruthy();
  });
});
